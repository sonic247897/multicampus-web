<!DOCTYPE html>
<html>
<head>
	<meta charset="EUC-KR">
	<title>Insert title here</title>
	<script type="text/javascript">
		function showTime(){
			today = new Date();
			str = today.getHours() + "시 ";
			str = str+today.getMinutes() +"분 ";
			str = str+today.getSeconds() + "초 ";
			myform.time.value = str;
		}
		function run(){
			// 1. 한줄 쓰기
			/* 주소표시줄 객체 = location */
			//window.setTimeout("location.href='https://www.naver.com/'", 3000);
			jobid = window.setInterval(showTime, 1000);
			// 참조형 변수 - window.setInterval호출문과 변수를 같이 사용하게 되면
			// 실행 중인 작업아이디를 변수에 저장하게 되므로 
			// 실행 중인 작업을 제어하기 편하다.
		}
		function stop(){
			//clearInterval을 이용해서 setInterval되고 있는 job을 제거할 수 있다.
			window.clearInterval(jobid);
			
		}
		
	</script>
</head>
<body onload= "showTime()">
	<form name = "myform" action= "">
		<input type="text" name="time"/>
		<input type="button" value="시작" onclick="run()"/>
		<input type="button" value="중지"/>
	</form>
</body>
</html>